#!/usr/bin/env python3
"""takes a str1 and a str2 as arguments and returns a concatenated string
"""


def concat(str1: str, str2: str) -> str:
    """combines the two strings
    """
    return (str1 + str2)
